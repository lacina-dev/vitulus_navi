# Audit navigačního a mapovacího stacku — 2026-08-01

4 paralelní audity (lokalizace, mapping, planner+launch, živý runtime) + ruční kontrola dock stacku.
Vše ověřeno proti kódu (file:line) a živému stavu; nic nebylo měněno.

## A. FUNKČNÍ BUGY (opravit)

### Datová korupce
- **A1 HIGH** `vitulus_mapping/nodes/mapping_manager` (~329-364, 586-599): stale `_pending_dock_wp` carry-over —
  docked stop bez rasteru nechá stashnutou pózu; PŘÍŠTÍ stop s rasterem ji publikuje pod jiným datumem
  → přepíše 'docked' waypoint starou souřadnicí. Fix: `_pending_dock_wp = None` v else-větvi `_verify_stop_in_dock`.
- **A2 HIGH** `vitulus_planner/nodes/node_planner:652-667 + 1965-1968`: smazaná zóna se VZKŘÍSÍ —
  `_bundle_export_zones` merguje by-name (smazanou zachová v geojson), import-if-newer ji vrátí. Chybí delete/tombstone.
- **A3 MED** `node_planner:1985-1989 vs 2057-2065`: export programů bez map_name filtru + import s filtrem →
  při smíšených tazích v pickle duplicitní programy pod aktivním tagem; program_select bere první shodu jména.
- **A4 MED** `node_planner:2101-2108`: import porovnává mtime bundlu se `saves/*.pkl`, zapisuje jen `running/` →
  bundle vždy "newer", plný re-import+regen všech zón při každém loadu.

### Lokalizace (gloc)
- **A5 HIGH** `gloc_server:678-679`: engage-delay je no-op (guard invertovaný — `_engage_delay_active()` vrací vždy False);
  korekce navíc gatované jen na `_tracker_should_idle()`, ne `_tracker_engaged()` (:1138). 5s anti-flap z 12.7. nikdy nefungoval.
- **A6 HIGH** `gloc_server:362-363, 1519-1535`: manuální `/gloc/apply` z UI publikuje do `/rtabmap/initialpose`
  (0 subscriberů, rtabmap neběží) a hlásí "APPLIED" — ruční relokalizace nedělá nic. Přesměrovat na `/nav_tf/set_map_pose_tracker`.

### Systém
- **A7 HIGH** `vitulus/nodes/main:50-54` + `/etc/systemd/system/vitulus.service`: `restart_core` = signal_shutdown,
  ale `Restart=always` je v service ZAKOMENTOVÁN → "Restart" trvale zabije stack (známý incident 18.7. s /main_node).
- **A8 MED** `navi_man:1099`: volání neexistující `publish_map_point_pose` (latentní AttributeError, topic nikdo nepublikuje — smazat sub+cb).
- **A9 MED** `navi_man:2033-2034`: edit_map "backup" kopíruje rtabmap.db do BACKUP_LAST.pkl (garbage záloha) a bez exists() → výjimka.
- **A10 MED** `navi_man:2071,2120`: `split("***env*")[1]` = IndexError na název mapy bez markeru (otevřený UI vstup).
- **A11 MED** `navi_man:2399→1104`: `add_autopath_point` bez try/except v 30Hz smyčce — TF výpadek shodí celý navi_man.
- **A12 MED** `navi_man:1207-1248, 1918-1953`: `wait_for_service` bez timeoutu v callbaccích (MBF params; `/rtabmap/set_mode_*`
  neexistují → každý UI edit-map visí 35 s). Podobně `navi_transform:535` (set_dock_pose čeká navždy bez navi_man).
- **A13 MED** mapping `trajectory_dem:127`: `gps_alt_ok` trvale False v z_mode=dr → UI celou session lživě "⚠ no RTK altitude".
- **A14 MED** `mapping_manager:496-522`: prázdná session čeká celý 25s snapshot timeout (direct_raster odmítne bez NACK);
  + cb_start/cb_stop race o self.proc bez zámku (stop_proc blokuje až 45 s).
- **A15 MED** `direct_raster:161-163`: live-toggle `use_lidar=true` po startu se persistne+hlásí, ale bez restartu nic nedělá (sub nevznikne).

### Costmap / recovery (bezpečnostní chování ≠ konfigurace)
- **A16 HIGH** `vitulus_local_costmap/src/local_costmap.cpp:187-237` + MBF StaticLayer trinary=true:
  node-side inflace (0.2m, cost 1-99) i gradual decay jsou ZAHOZENY (vše <100 → FREE). Překážka zmizí po PRVNÍM
  decay kroku, inflační smyčka = spálené CPU.
- **A17 MED** `recovery_behavior.yaml:34-40` + `smach_exe_goal:94`: `clear_costmap_static` maže static_layer,
  což o 15 řádků výš tentýž soubor zakazuje ("planner projede zdí"); local polovina no-op (jméno pluginu nesedí).
- **A18 MED** inflation_radius: global 0.85 (legacy top-level param vyhrává) vs local 0.5 vs common 0.5 — sjednotit vědomě.
- **A19 MED (redesign)** `local_costmap_params.yaml:2`: rolling local costmapa v `global_frame: map` —
  každá map→odom korekce posune okno vůči překážkám; standard je `odom`. Souvisí s pose-jump incidenty.

## B. RUNTIME — CPU saturace v doku (load avg 15.9!)

| Node | CPU | Pozn. |
|---|---|---|
| dock_detector | 122 % | v doku nepublikuje NIC, plně žere scan+odom+tf |
| vo_rgbd_odometry | 107 % | RGBD VO naplno (má /pause_odom službu!) |
| docking_controller | 42 % | idle |
| move_base_flex | 37 % | hlásí "Map update loop missed 10Hz" = důsledek saturace |
| realsense manager | 36 % | + `/d435/depth/color/points` 15 Hz bez jediného subscriberu |
| local_costmap_node | 36 % | 20×20m grid, MBF okno 7×7 → ~88 % decay práce mimo okno |

rosout spam: lidar_icp ~9 msg/s + VO ~5.7 msg/s (INFO per-frame; řešit log-levelem rtabmap_ros.Odometry→WARN).
Diagnostics trvale: gnss "Timestamps too far in future" ERROR 3 Hz; oba EKF "Erroneous data or settings"; NTP offset.
**Doporučení: dock-idle režim** — pause VO/ICP (/pause_odom), spánek dock_detectoru při `is_in_dock`, vypnout
realsense pointcloud, zvážit obstacles pipeline v doku. Uvolní ~2+ jádra.

## C. MRTVÁ VÁHA (bez změny chování smazat/pohřbít)

- **Celé mrtvé balíčky**: `vitulus_bringup` (13 launchů, vč. pasti vitulus_d435.launch + mrtvá kopie service/startup),
  `vitulus_slam` (13 launchů; rtabmap jede z docker image), `vitulus_webui` (nahrazen vitulus_ui).
  → CATKIN_IGNORE / přejmenovat `_dead_`, nemazat hned.
- **Mrtvé launche jednotlivě**: vitulus/{main,open_cr,t265}.launch, vitulus_imu/{v2,madgwick}, vitulus_gnss/mapviz, teleop_key.
- **navi_transform ~350 ř.**: set_wheel_base_pose (T1 — jediný porušovatel "wheel EKF never set_pose"!), 3× 30Hz odom
  publishery pro nikoho (T2), imu_baselink (T3), služby reset/set_nav_wheel_odom (T4), rewind splice za vypnutým flagem (T5),
  mrtvé stavové proměnné (T15). Shadow bridge ~400 ř. — viz E1.
- **gloc_server ~250 ř.**: relock mašinerie za relock_enable=false (vědomě, ale mrtvá), defer_to_rtabmap, auto_apply.
- **dock_localization_seed ~150 ř.**: celá rtabmap Layer-2 větev (sub bez publishera, confidence timer navěky False),
  _ekf_set_pose_srv nevolaná, verify-reseed pozůstatky.
- **navi_man ~200 ř.**: set_map_utm_transform (sendTransform ZAKOMENTOVÁN — 30Hz práce zahozena + hasattr('a.b') bug),
  publish_pose_cov_stamped 30 Hz pro nikoho, movebase_client (move_base neexistuje), waypoint_list_pub, …
- **insertion_gate**: octomap-forwarding pozůstatky (tf_bcast, _cap_cloud_range, …), inertní set_ranges/range_overrides.json.
- **trajectory_dem**: `/mapping/terrain` GridMap 5 s pro nikoho (drží grid_map_msgs závislost); demgrid band-éra funkce (drží scipy).
- **node_planner**: ros2d éra ~250 ř. (connect_near_multilines, …), těžké importy geopandas/matplotlib (sekundy bootu),
  6× /web_plan/* publishery bez konzumenta, python2 bytecode `node_plannerc`.
- **MBF config**: TrajectoryPlannerROS nakonfigurovaný a nikým nevolaný; dwa/graceful/neo params (~250) pro neexistující
  kontrolery; laser_layer blok bez pluginu; subscribe_to_updates bez updates topiců.
- **UI (mapping.js)**: band-éra publishery /mapping/{save,regenerate,compare,clear,set_band} — mrtvé topicy s živým
  advertise; "press Snapshot" hint na skryté tlačítko (mid-session snapshot fakticky nemožný — /mapping/save_direct z UI nejde).
- **~135 `.bak_*` souborů** v živých adresářích (mapping ~60, navi ~75) + `_bak_rainzoom_20260717/` v UI templates.
- Drobné: `max_maporr_rate_mps` překlep (funkční, všude konzistentní), XML smetí v vitulus_navi_mbf.launch,
  robot_description se při bootu vyhodnocuje 2× (vyhrává base_control verze — past na budoucí xacro fixy).

## D. EFEKTIVITA (kód)

- `direct_raster:479-514`: bez dirty flagu renderuje celý grid 2× za tick (render+stats) + `tolist()` milionů buněk
  i beze změny. → dirty flag, jeden raster() na tick.
- `node_planner:2469-2480`: 2Hz `tolist()` celého gridu pro map_assembled/map_edited, které NIKDO neodebírá; simplify()
  všech polygonů každý tick. Živý je jen map_show.
- `node_planner:2369-2377`: každá změna site_map regeneruje VŠECHNY zóny + per-zóna save_programs (N× pickle+yaml export).
- `insertion_gate:363-375`: projectLaser na každý scan PŘED throttlem, výsledek jen pro počítadla; dvojí deserializace
  týchž mračen ve 2 procesech; CSV log plným tempem při zavřené gate s flush() po řádku.
- `navi_man` 30Hz smyčka: mrtvá práce (C) + re-publish statického datumu (stačí latched 1 Hz); get_maplist stat 5 s;
  _build_bootstrap_map blokuje smyčku 3 s wait_for_message.
- `navi_transform`: wait_for_service+nová proxy při každém volání v 10Hz hot path (T9); čas přes int(str(Time.now())).
- `gloc_server`: blokující TF lookupy v 5Hz timeru (1s timeout; statická lidar→base transformace řešitelná jednou).
- TEB na 30 Hz (běžně 5-10); global_planner publish_potential bez odběratelů.

## E. REDESIGN / MIGRACE

- **E1** Dokončit migraci `bridge_status`→`loc_status` (gloc_server:404, dock_localization_seed:329 string-parsují
  'gps_good=1') → pak jde zabít shadow bridge v navi_transform (~400 ř. na 30 Hz, jehož jediná živá funkce je výroba stringu).
- **E2** Site-native gate na legacy `load_map`/`load_map_rtabmap` v navi_man (M9/M10): dnes jeden UI publish tiše vrátí
  legacy svět waypointů/datumu + nastartuje vyřazený rtabmap mapping. logwarn-reject za legacy_maps=false.
- **E3** Zámky: navi_man `running_map_data` (30Hz publikuje půl-vynulovaný georef při load), planner worker vs callbacky
  (zóny), gloc `_rms_baseline` (EMA starého rasteru po výměně mapy).
- **E4** insertion_gate zeštíhlit na "gate+datum server" (bez cloud/laser subscribů).
- **E5** Dock-idle režim (viz B). dock_detector (map-based modalita) vs docking_controller (intensity) — zvážit, zda
  map-based modalitu ještě držet.
- **E6** navi_transform spawnuje EKF pár ve vlastním __init__ → jeho respawn = restart EKF + reset pózy (vědomé rozhodnutí?).
- **E7** Sjednotit per-site overrides: range_overrides.json (mrtvý) vs direct_overrides.json (živý).

## Zdravé (ověřeno, neměnit)

Launch řetěz sedí 1:1 na běžící nody; žádné mrtvé služby v runtime; EKF kovarianční fixy konzistentní s kódem;
prahy insertion_gate↔direct_raster konzistentní by-construction; stop/save race (docked PO auto-serve) správně
až na A1; dock confirm funguje (is_in_dock_confirmed=True); bundle svět soběstačný; observation_sources costmap
vrstev všechny živé; TEB rychlostní profily = záměrný dynamic reconfigure.

---
# STAV OPRAV — 2026-08-01 večer (vše 1-6 IMPLEMENTOVÁNO)

**Fáze 1 (bugy):** A1 ✔ (stash cleared v obou větvích), A2 ✔ (_bundle_remove_zone delete path),
A3 ✔ (dedupe by name na importu; + živý úklid: programs.yaml i pickle měly každý program 2×
— deduped 20→10, záloha programs.yaml.bak_dedup_20260801), A4 ✔ (bundle_import_stamp.json),
A5 ✔ (invertovaný guard + gate na _tracker_engaged), A6 ✔ (apply → set_map_pose_tracker),
A7 ČEKÁ NA SUDO (viz níže), A8-A12 ✔ (mrtvý sub pryč, backup fix, split guardy, timeouty),
A13 ✔ (dr = zdravý z-zdroj), A14 ✔ (save_direct_ack NACK), A15 ✔ (lidar sub per-callback gate),
A16 ✔ (trinary_costmap:false na cloud+lidar vrstvách), A17 ✔ (clear_costmap_static →
live vrstvy), A18 ✔ (inflation 0.85 deklarované per-layer), A19 ✔ (local costmap frame odom).

**Fáze 2 (dock-idle):** nový nód vitulus_navi/nodes/dock_idle (pause/resume /vo+/licp přes
per-node remapy, v navi_man.launch; teď běží ručně spuštěný stopgap), dock_detector +
docking_controller vlastní idle gate (POZOR: dock_status je latched → promote přes 2s Timer,
ne přes message arrival — první verze na to narazila), d435 enable_pointcloud=false (VYŽADUJE
plný restart stacku). VO: 107→2 % CPU. Load 15.9 → ~4.

**Fáze 3 (úklid):** CATKIN_IGNORE v bringup/slam/webui; 317 .bak → _dead_20260801/ per balíček;
navi_transform: T1-T5+T15 pryč (set_wheel_base_pose, 3× 30Hz odom pub, imu_baselink, služby
reset/set_nav_wheel_odom, offsety, fix_usable_history); insertion_gate: octomap zbytky pryč;
node_planner: ros2d řetěz + geopandas/matplotlib/resource importy + 6 mrtvých /web_plan
publisherů pryč (POZOR: /web_plan/map_edited ŽIJE — čte ho navi_man! audit se tu mýlil);
UI: lživé „press Snapshot" hinty přepsány. Band-éra JS publishery ponechány (skrytá tlačítka,
advertise zdarma — vytržení = riziko bez zisku).

**Fáze 4 (migrace):** gloc_server + dock_localization_seed čtou /nav_tf/loc_status (JSON);
shadow bridge ZAPARKOVÁN za ~bridge/compute_enable=false (rollback = flip na true);
site-native gate na load_map/load_map_rtabmap (reject při legacy_maps=false).

**Fáze 5 (zámky):** navi_man _map_data_lock (georef read + clear + pickle swap + bundle datum
refill), planner _workspace_lock (refresh worker vs save/remove zone) + alias fix (list copy)
+ restore na rebuild fail, gloc _rms_baseline EMA pod _lock.

**Fáze 6:** local costmap global_frame: map→odom (rollback v komentáři). Restart: navi_man
(vzal s sebou MBF/VO/ICP → nové configy+remapy), navi_transform (EKF pár), gloc, planner,
mapping_manager, dock nody, webnode. Živě ověřeno: loc_status owner=dock, dock seed
confirmed+seeded, site servírován, MBF params (odom frame, trinary false, inflation 0.85,
recovery bez static_layer), /vo/pause_odom + /licp/pause_odom existují, dock_idle paused=true.

**ZBÝVÁ (vyžaduje usera):**
1. `sudo cp /tmp/claude-1000/-home-vitulus-catkin-ws/b9e5aeed-b454-4ce2-92cf-7e27f52def23/scratchpad/vitulus.service.fixed /etc/systemd/system/vitulus.service && sudo systemctl daemon-reload`
   (A7: Restart=always + RestartSec=5)
2. `sudo systemctl restart vitulus` (aktivuje: d435 pointcloud off, dock_idle z launch místo
   stopgapu, čistý start všech nodů s novým kódem)
3. Validační jízda: costmap frame odom + heading anchor v2 + direct mapper params (loc_eval
   recorder ZAPNOUT); po jízdě zvážit relock re-enable.
Pozn: zahrada2026_3 nemá zones.geojson (zóny zůstaly na retirovaném situ zahrada/zahrada2026)
— programy mají prázdné zone_listy už od 19.7.; vyřeší plná pokrývací jízda + nové zóny.

**DODATEK — skutečný kořen dock_detector CPU (py-spy):** node-level gaty nestačily —
hotspot byl VNITŘNÍ timer DockNavigatoru (dock_navigator.py timer_callback →
navigate_to_dock → check_obstacles = cv2 přes celou mapu, běžel navždy jakmile
navigation_possible=True, dokovaný nedokovaný). Fix: navigator.idle flag řízený
z dock-idle gatu rodiče (guard `idle and not navigation_running` — aktivní navigaci
nikdy nepřeruší). Výsledek ověřen živě: dock_detector 122 % → 4,3 %, load 15.9 → ~3.5.

**RUBBER-DUCK REVIZE (2026-08-01 pozdě večer):** (1) docking_controller
process_detection ODGATOVÁN (user requirement + poctivost: dock_manager confirm
cross-checkuje geometry přes freshness gate a gatovaná detekce mu podsouvala
zamrzlé hodnoty pod čerstvým streamem; primární power+reflektor cesta gatovaná
nikdy nebyla — lidar_intensity_dock_filter je samostatný netknutý nód). Živě:
dock_detected=True, distance 0.225 m, confirm=True. Idle-gated zůstávají jen
image/markery. (2) map_status falešný poplach — starý set_map_utm_transform měl
jen redundantní zápis, živý setter set_map_status() ve smyčce nedotčen.
(3) mapping ack ns sedí (/mapping/save_direct_ack), dock_idle stopgap reparented
na init (přežije shell). (4) MBF „Map update loop missed": 0 od restartu (dřív
spam) — CPU úleva potvrzená i tady. (5) Nový drobný šum: pauznuté VO/ICP hlásí
throttled „Did not receive data since 5 seconds" (~3/12 s) — kosmetické,
případně řešit log-levelem, řádově méně než dřívější per-frame spam.

---
# RELOCK V2 — 2026-08-02 (implementováno, čeká na kidnap test)

gloc_server (záloha gloc_server.bak_relockv2_20260802), inspirace RTAB-Map
LoopGPS/OptimizeMaxError (viz NAVI_RESEARCH_2026-08-02.md). Tři ploty:
1. RADIUS GATE: kandidát musí ležet v kruhu kolem aktuální (driftující) pózy —
   radius = relock_radius_base_m(1.5) + relock_radius_per_m(0.5) × ujetá
   vzdálenost od ztráty (z wheel odom, _lost_travel_m), cap 8 m. 4.68m teleport
   by zemřel tady.
2. ABSOLUTNÍ KVALITA: rms ≤ baseline×1.5 (stávající e0d8fc5) + NOVĚ overlap_frac
   ≥ relock_overlap_min(0.60) — šťastné rms na pár bodech nestačí.
3. PROBACE: kandidát se NEaplikuje hned — uloží se s odom pózou; každý tick se
   odometrií predikuje dopředu a přeskóruje živým skenem. Potřebuje
   relock_probe_ticks(3) potvrzení přes ≥ relock_probe_travel_m(0.3) jízdy;
   jediný fail = drop. Velký skok (>relock_big_delta_m) bez korroborace
   (dock/GPS) = probace ×relock_bigdelta_probe_mult(3) místo dřívějšího flat
   odmítnutí. Aplikuje se PREDIKOVANÁ póza (ne stará z capture). Probace se
   ruší: timeout 60 s, výměna rastru, návrat GPS.
Probační matematika ověřena offline testem. relock_enable ZŮSTÁVÁ false —
zapnout až po kidnap testu na dvoře (postavit robota "ukradeného", ověřit
správný relock + odmítnutí falešného kandidáta).

---
# VALIDAČNÍ JÍZDA 2026-08-02 22:19-22:48 — PROŠLA (bag validacni_jizda_*.bag, 77MB)
Heading anchor v2: před field-fixem (spread 1.5→6°, navi_transform restart v min
~22 jízdy) frozen 20 % času; PO fixu 0 % (449 s čistých). DVŮR: tracker ownership
min 11.4-13.0 + 16.3-16.6; drift report 129.8 s outage / 11.29 m blind → 0.056 m
chyba (vs. metry v éře 19.7.!). RTK 58 % FIXED / 35 % FLOAT / sats med 24 (noc).
Owner: dock 252s / gps 642s / tracker 122s / dr 752s — čisté přechody, žádný
teleport. Docking: 1. pokus padl na starých 180 s (v bagu), po 420 s zadokováno
+ confirmed. COMMITY: 8 repos (4458925, 8d36bd2, 6d1f6a7, 5bc7a63, b748554,
fcd241c, d5cae5e, 29e0161). OTEVŘENÉ: UI costmap render posunutý (ros3d TF
s odom frame — jen zobrazení, plánování OK), kidnap test relock v2, flash 1.51.
