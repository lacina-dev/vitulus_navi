# Validace 2026-07-18 — vše najednou (po celodenní implementaci)

_Co se dnes změnilo: 8 lokalizačních fixů, 13 mapping fixů, site bundle
(UTM zdroj pravdy: waypointy/cesty/zóny/programy/edity per site), edits-mask
kompozice do site_map, editor map v UI, editor waypointů, PWA/fullscreen fixy,
forensní recorder topicy. Vše commitnuto v 6 repech, migrace novamapa applied._

## A. Smoke po restartu (dělá Fable automaticky)
- [ ] robot_health OK (rtabmap node down je OK — mimo pózovou cestu)
- [ ] D435: filtry aktivní (`rosparam get /d435/`…, pořadí decimation→disparity→spatial→temporal→hole_filling)
- [ ] loc_status: owner=dock, docked; dock seed proběhl
- [ ] site_map servírován (novamapa/final_…), status má `edits_applied: 0`
- [ ] `/mapping_manager/edit_list` latched `[]`
- [ ] navi_man log: bundle site=novamapa; import waypointů z bundlu (1× docked, pozice ≈ pickle)
- [ ] planner log: import zón (7) + programů (10) z bundlu, regenerace drah, ŽÁDNÝ warn „area/length drifted >5%"
- [ ] tracker `/gloc/track_status`: rms < 0.1 u doku, žádný lost
- [ ] UI :7779 běží, Edit map tlačítko aktivní (site served)
- [ ] `/nav_tf/set_pose` služba funkční (test call s aktuální pózou — NEDĚLAT za jízdy)

## B. UI / editor (uživatel, ~10 min, robot v doku)
1. Mapping tab → **Edit map**: nakreslit obstacle polygon → Finish → objeví se
   v overlay I v site_map rastru; `edit_list` má záznam; `edits_applied: 1`.
2. Wall šířka 0.3 → ribbon správné šířky. Delete edit → zmizí. Clear all.
3. Waypoint tool: umístit `test_wp` (yaw slider), Move, Delete. Ověřit
   `waypoints.geojson` se aktualizuje (dual-write).
4. Zóna: otevřít existující (FakeTree…), drobná úprava → Save → zones.geojson
   mtime se změní; dráhy v UI vypadají rozumně.
5. **PŘED spuštěním programu**: vizuálně zkontrolovat dráhy programu na mapě
   (regenerované z UTM — poprvé ve správném frame GARDEN_2026, dřív byly
   v frame staré mapy 2025_06!).
6. Telefon: A2HS/fullscreen, otočení, klávesnice (dvh fix — už nemá mačkat mapu).

## C. Mapovací validační jízda (kamera filtry POPRVÉ aktivní)
- [ ] Nová session (nový site, ne přes novamapa!) — krátká jízda zahradou
  vč. garáže (kotvené průjezdy start/konec v RTK FIXED)
- [ ] Sledovat: `gate_status` → `datum_samples` roste, `stamped_reject`
  counter (poprvé měřitelný), `passed`/`throttled` poměry
- [ ] `/mapping/z_corr` na 5 Hz (nový topic)
- [ ] Po Stop: raster servable, datum_warning NENÍ, žádný final_*.ot navíc
- [ ] A/B kvalita vs novamapa: méně šumu (D435 filtry), garáž ostřejší
- [ ] Depth šum na slunci — subjektivně vs minule

## D. Lokalizační jízda (dohromady s C, gnss recorder ZAPNOUT)
- [ ] Recorder nahrává nové topicy (bridge_status, loc_status, track_status,
  set_map_pose*, /scan @5fps, odom_bridge_preview)
- [ ] Pod střechou: tracker engage, korekce plynulé (two-speed slew)
- [ ] GPS výpadky: drift_report JEN u reálných výpadků (>3 s / >0.3 m) —
  žádný spam při RTK flapu
- [ ] Návrat do doku: re-anchor < 0.10 m; set_dock_pose tlačítko v UI už
  NEteleportuje na approach bod (oprava F1)
- [ ] loc_status ownership přechody čisté (žádné 12s díry)

## E. Otevřené k prošetření (nový nález z auditu)
- [ ] **Dock seed jede trvale UNCONFIRMED** — `/dock_manager/is_in_dock_confirmed`
  = False i v doku (power říká docked). Guard z WP3 tak reálně nechrání.
  Prošetřit dock_detector geometrii/reflektor u doku (večer/zítra).
- [ ] Programy: po ověření drah v UI zvážit „re-save" všech 10 programů
  (přepíše globální pickle čistými GARDEN_2026 snapshoty).

## Rollback
- Bundle: smazat `~/.vitulus/mapping_v3/novamapa/{manifest.yaml,*.geojson,programs.yaml}`
  → vše se vrátí na pickle svět (import/export se vypne).
- Kód: `.bak_locfix_*`, `.bak_mapfix_*`, `.bak_bundleB/C_*`, `.bak_editmask_*`,
  `.bak_editor_*`, `.bak_uifix_*` zálohy vedle souborů + git commity per repo.
