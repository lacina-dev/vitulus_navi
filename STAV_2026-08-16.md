# STAV 2026-08-16 — číst PRVNÍ při navázání (nahrazuje STAV_2026-08-03.md)

Souhrn stavu po víkendu 2026-08-15/16. Blížíme se k hotové navigaci a mapování.

## Co je HOTOVÉ a funguje

### Mapa a mapování (v3)
- **Nmap = plná mapa zahrady AKTIVNÍ** (32.7 m², verze `direct_20260816_200330`).
  Nahraná s opraveným snímáním, anti-erozí a živým laděním. Hustá — ideální pro
  lidar tracker.
- **Anti-eroze validována**: `lidar_free_dec` (slabé mazání lidar paprsky),
  `occ_erode_factor` (hystereze potvrzených buněk), presety s pravidlem
  miss_dec ≤ hit_inc. Mapované objekty drží.
- **Živé ladění překážek při mapování**: slidery + presety (Lawn short/tall,
  Paved yard, Bushes/edge) v UI, wide segmentační větev v Dockeru
  (`/mapping/obstacles_cloud_wide`, 0.05–2.0 m, 4 m depth). Navigační úzká
  větev netknutá.
- **Session ovládání**: Start recording / Save & finish / Erase & keep
  recording / Discard. Vše anglicky, status header (Active map / ● Recording).
- **Factory-fresh režim**: Deactivate → žádná mapa, marker `{"none":true}`
  přežije boot (navi_man serve-retry ho respektuje).
- **Memoryless globální costmapa**: `NonPersistentVoxelLayer` — značí jen
  aktuálně viděné. POZOR: **MBF nemá respawn** — `rosnode kill` ho zabije
  natrvalo, obnova jen restartem stacku!

### Lokalizace
- **Undock tracker fix** (2026-08-15, commitnuto): tracker korekce potlačeny
  u docku (dock_status 0/1/2 + grace 8 s), sanity mez 1.5 m/30°, GPS probace
  re-arm na undock hraně, dock jen map→odom. AKTIVNÍ (stack restartován).
- **Drift-budget gate + weak-axis projekce** (commity a402de4, 11cdc85):
  tracker korekce omezené rozpočtem driftu od poslední důvěryhodné kotvy
  (0.15 m + 6 %/m + rotační penalizace); gloc EDT tracker promítá korekce
  na silnou osu při degenerované geometrii. NASAZENO — **POLNÍ TEST ČEKÁ**
  (viz Co zbývá, bod 1).
- GPS cukání opraveno (stamp párování, deadband/slew — BAG_ANALYZA_2026-08-07.md).

### UI (vitulus_ui :7779)
- Map tab kompletně předělaný (EN), Saved maps se badge ACTIVE/● + Activate,
  aerial georef automatiky (datum re-fetch, cross-map preview přes UTM).
- Editor waypointů: živá yaw šipka + číselný vstup 0.5°, Move prefill,
  tmavě modré waypointy s halo.
- Vrstvení: dock intensity nad mapami, robot polygon úplně navrchu
  s konfigurovatelnými barvami (Display → Robot marker).
- Myš: rotace kolem Z opravená (stateful theta + tangent up), zoom jen
  kolečkem, obráceně.
- Kamera HUD: lazy stream (jen při zobrazení + viditelném tabu), loader,
  klik = zoom (malá 141×106 vlevo dole u logu / velká nad logem), auto-resume.
- Klientská nastavení persistentní (localStorage, ~20 klíčů).

### Večerní bug-hunt 2026-08-16 (oba VYŘEŠENÉ)
1. **TF goal leak**: každý reload UI vytvořil tf2_web_republisher actionlib
   goal, který se nikdy nezrušil → 61 živých goalů × 20 Hz ≈ 1200 msg/s →
   rosbridge 68 % CPU → scan/pointcloudy se vykreslovaly s replay zpožděním
   („lidar jede jinam", polygon OK). FIX: `groovyCompatibility:false` →
   TFClient přes /republish_tfs SERVICE (mrtvé taby se uklidí samy do 10 s),
   respawn pro republisher (commit 961a507 ve vitulus_ui). rosbridge ~20 %.
2. **Kamera po restartu robota nenavázala**: mjpegcanvas `changeStream` dělá
   pokaždé `new Image()` a starý s visícím /stream spojením opustí → při bootu
   (web_video_server čeká na d435 topic) každý retry leakl spojení → Chrome
   pool (6/host) vyčerpán → kamera mrtvá navždy. FIX v `reloadStream`: abort
   starého streamu před novým + killcache + forced watchdog à 3 s.
   **Ověřit při příštím restartu robota.**

## Co ZBÝVÁ (příští víkend)

1. **PRIORITA: polní test drift-budget + weak-axis u docku na Nmap**
   — lokalizace bez RTK/ve tmě („Popojel jsem dopředu z docku a zas skoro
   zpátky…to musí fungovat"). Nmap je hustá = ideální podmínky. Monitoring
   připravený — říct Claudovi, vyjet z docku, popojet a vrátit se.
2. **Boot-heading race P1–P3** — dokumentováno v memory
   undock-tracker-fix-2026-08-15, čeká na polní reprodukci.
3. **Heading přijímač**: ověřit carrier FIXED pod otevřeným nebem (během
   16.8. se sám uzdravil, ale měl výpadek).
4. **Kidnap test** (ze stack auditu 08-01) — reflector posts jako noční cesta.
5. **Flash GNSS 1.51** — čeká na Windows stroj (wine serial mrtvý).
6. **Necommitnuté soubory**: `vitulus_ui/nodes/templates/assets/js/map_view.js`
   + `app.js` — nesou settings-persistence práci PARALELNÍ session + moje
   fixy (groovyCompatibility, kamera reconnect). Nekomitovat cizí práci bez
   domluvy; až paralelní session dokončí, commitnout spolu.
7. Stará `vitulus_webui` (:7778?) pořád leakuje TF goaly — legacy, netknutá.
   Kdyby se používala, aplikovat stejný groovyCompatibility fix.

## Pravidla (trvalá)
- **Žádný push** do remotes. Commity per-feature ano.
- **Vše v UI anglicky.**
- **O pauzách/konci rozhoduje uživatel** — nenavrhovat končení.
- catkin python stuby: edity v src jsou živé po restartu nodu, NIKDY necpat
  soubor přes stub. webnode restart jen `kill -9` (vzor z proměnné, ne přímo
  v pgrep!). mapping_manager je perzistentní.
